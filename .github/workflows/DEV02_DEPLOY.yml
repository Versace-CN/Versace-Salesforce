# Salesforce 开发环境自动化部署工作流（developer分支 → dev02环境）

name: Salesforce Dev02 Environment Deployment

# 触发器配置：当代码推送到main分支时触发
on:
  push:
    branches:
      - main
    # 只监控相关的文件变化
    paths:
      - 'manifest/package.xml'
      - 'force-app/**'
      - '.github/workflows/**'

# 环境变量配置
env:
  # 基础路径配置
  MANIFEST_DIR: 'manifest'
  PACKAGE_FILE: 'manifest/package.xml'

# 工作流任务定义
jobs:
 
  # 第一阶段：部署准备与配置加载
  prepare-deployment:
    name: 1. 部署准备与配置
    runs-on: ubuntu-latest
 
    # 输出变量
    outputs:
      has-valid-package: ${{ steps.check-package.outputs.has-valid-package }}
      package-file-path: ${{ steps.check-package.outputs.package-file-path }}
      test-classes: ${{ steps.load-test-config.outputs.test-classes }}
      test-level: ${{ steps.load-test-config.outputs.test-level }}
      should-run-tests: ${{ steps.load-test-config.outputs.should-run-tests }}
 
    steps:
 
    # 步骤1：检出代码
    - name: 🔍 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        ref: ${{ github.ref }}
 
    # 步骤2：显示环境信息
    - name: 🌐 环境信息
      run: |
        echo "=== 开发环境部署 ==="
        echo "分支: developer"
        echo "环境: dev02"
        echo "目标组织: dev02-org"
        echo "==================="
 
    # 步骤3：检查package.xml文件
    - name: 📦 检查部署清单
      id: check-package
      run: |
        echo "检查package.xml部署清单..."
 
        # 基本文件检查
        if [ ! -f "${{ env.PACKAGE_FILE }}" ]; then
          echo "❌ 错误: package.xml文件不存在"
          echo "has-valid-package=false" >> $GITHUB_OUTPUT
          exit 1
        fi
 
        # 检查文件内容
        if ! grep -q "<Package" "${{ env.PACKAGE_FILE }}" || [ ! -s "${{ env.PACKAGE_FILE }}" ]; then
          echo "❌ 错误: package.xml格式不正确或为空"
          echo "has-valid-package=false" >> $GITHUB_OUTPUT
          exit 1
        fi
 
        # 统计组件数量
        COMPONENT_COUNT=$(grep -c "<members>" "${{ env.PACKAGE_FILE }}" || echo "0")
        if [ "$COMPONENT_COUNT" -eq 0 ]; then
          echo "❌ 错误: package.xml中没有部署组件"
          echo "has-valid-package=false" >> $GITHUB_OUTPUT
          exit 1
        fi
 
        echo "✅ package.xml检查通过"
        echo "📦 部署组件: $COMPONENT_COUNT 个"
        echo "has-valid-package=true" >> $GITHUB_OUTPUT
        echo "package-file-path=${{ env.PACKAGE_FILE }}" >> $GITHUB_OUTPUT
 
    # 步骤4：加载测试配置（仅DEV02_TEST_CLASSES和DEV02_TEST_LEVEL）
    - name: ⚙️ 加载测试配置
      id: load-test-config
      run: |
        echo "加载开发环境测试配置..."
 
        # 从GitHub Variables获取配置
        DEV02_TEST_CLASSES="${{ vars.DEV02_TEST_CLASSES || '' }}"
        DEV02_TEST_LEVEL="${{ vars.DEV02_TEST_LEVEL || 'NoTestRun' }}"
 
        echo "📋 原始配置:"
        echo "- DEV02_TEST_CLASSES: '$DEV02_TEST_CLASSES'"
        echo "- DEV02_TEST_LEVEL: '$DEV02_TEST_LEVEL'"
 
        # 清理和验证TEST_CLASSES（空格分隔）
        # 去除首尾空格，将多个空格合并为一个空格
        CLEANED_TEST_CLASSES=$(echo "$DEV02_TEST_CLASSES" | xargs)
 
        # 验证TEST_LEVEL有效性
        VALID_TEST_LEVELS=("NoTestRun" "RunSpecifiedTests" "RunLocalTests")
        VALID_LEVEL=false
 
        for LEVEL in "${VALID_TEST_LEVELS[@]}"; do
          if [ "$DEV02_TEST_LEVEL" = "$LEVEL" ]; then
            VALID_LEVEL=true
            break
          fi
        done
 
        if [ "$VALID_LEVEL" = false ]; then
          echo "❌ 错误: DEV02_TEST_LEVEL值无效"
          echo "有效值: NoTestRun, RunSpecifiedTests, RunLocalTests"
          echo "当前值: $DEV02_TEST_LEVEL"
          exit 1
        fi
 
        # 确定是否执行测试
        SHOULD_RUN_TESTS="false"
        if [ "$DEV02_TEST_LEVEL" = "RunSpecifiedTests" ] && [ -n "$CLEANED_TEST_CLASSES" ]; then
          SHOULD_RUN_TESTS="true"
          echo "✅ 将执行指定测试类"
        elif [ "$DEV02_TEST_LEVEL" = "RunLocalTests" ]; then
          SHOULD_RUN_TESTS="true"
          echo "✅ 将执行所有本地测试"
        else
          echo "ℹ️  不执行测试 (TEST_LEVEL: $DEV02_TEST_LEVEL)"
        fi
 
        # 验证测试类格式（如果配置了RunSpecifiedTests）
        if [ "$DEV02_TEST_LEVEL" = "RunSpecifiedTests" ]; then
          if [ -z "$CLEANED_TEST_CLASSES" ]; then
            echo "❌ 错误: TEST_LEVEL为RunSpecifiedTests但未指定测试类"
            echo "请在DEV02_TEST_CLASSES中指定测试类（空格分隔）"
            exit 1
          fi
 
          # 检查测试类格式（只能包含字母、数字、下划线）
          for TEST_CLASS in $CLEANED_TEST_CLASSES; do
            if ! [[ "$TEST_CLASS" =~ ^[a-zA-Z_][a-zA-Z0-9_]*$ ]]; then
              echo "❌ 错误: 测试类名格式无效: $TEST_CLASS"
              echo "测试类名只能包含字母、数字、下划线，且不能以数字开头"
              exit 1
            fi
          done
 
          echo "✅ 测试类格式验证通过"
          echo "测试类列表: $CLEANED_TEST_CLASSES"
        fi
 
        # 输出结果
        echo "📋 最终测试配置:"
        echo "- 测试级别: $DEV02_TEST_LEVEL"
        echo "- 测试类: $CLEANED_TEST_CLASSES"
        echo "- 是否执行测试: $SHOULD_RUN_TESTS"
 
        # 设置输出变量
        echo "test-level=$DEV02_TEST_LEVEL" >> $GITHUB_OUTPUT
        echo "test-classes=$CLEANED_TEST_CLASSES" >> $GITHUB_OUTPUT
        echo "should-run-tests=$SHOULD_RUN_TESTS" >> $GITHUB_OUTPUT

  # 第二阶段：环境配置与部署验证
  configure-and-validate:
    name: 2. 环境配置与部署验证
    runs-on: ubuntu-latest
    needs: prepare-deployment
 
    # 条件：package.xml有效
    if: ${{ needs.prepare-deployment.outputs.has-valid-package == 'true' }}

 # 输出变量
    outputs:
      DEPLOY_ID: ${{ steps.validate-deploy.outputs.deploy-id }}
      
    steps:
 
    # 步骤1：检出代码
    - name: 🔍 检出代码
      uses: actions/checkout@v4
 
    # 步骤2：设置Node.js环境
    - name: ⚙️ 设置Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 'latest'
 
    # 步骤3：安装Salesforce CLI
    - name: 📦 安装Salesforce CLI
      run: |
        echo "安装Salesforce CLI..."
        npm install -g @salesforce/cli
        sf --version
        sf plugins install @salesforce/plugin-packaging @salesforce/plugin-source --force
 
    # 步骤4：登录到Salesforce DEV02
    - name: 🔐 登录到Salesforce DEV02
      run: |
        # 登录到Salesforce
        echo "登录到dev02环境..."
        echo '${{ secrets.SFDX_DEV02_ORG_AUTH }}' > ./SFDX_ORG_AUTH.json
        sf org login sfdx-url --sfdx-url-file ./SFDX_ORG_AUTH.json --set-default
 
        echo "✅ 认证成功"
 
    # 步骤5：执行部署验证（dry-run）
    - name: 🧪 验证部署
      id: validate-deploy
      run: |
        echo "执行部署验证（dry-run）..."
 
        # 获取测试配置
        TEST_LEVEL="${{ needs.prepare-deployment.outputs.test-level }}"
        TEST_CLASSES="${{ needs.prepare-deployment.outputs.test-classes }}"
 
        echo "测试配置:"
        echo "- 测试级别: $TEST_LEVEL"
        if [ -n "$TEST_CLASSES" ]; then
          echo "- 测试类: $TEST_CLASSES"
        fi
 
        # 构建验证命令
        VALIDATE_CMD="sf project deploy start --concise -w 60 --dry-run --manifest ${{ needs.prepare-deployment.outputs.package-file-path }} --test-level $TEST_LEVEL -o ${{ vars.DEV02_USER_NAME }} --json"
 
        # 如果是指定测试类，添加到命令
        if [ "$TEST_LEVEL" = "RunSpecifiedTests" ] && [ -n "$TEST_CLASSES" ]; then
          VALIDATE_CMD="$VALIDATE_CMD --tests $TEST_CLASSES"
          echo "转换后测试类: $TEST_CLASSES"
        fi
 
        echo "执行命令: $VALIDATE_CMD"
 
        # 记录开始时间
        START_TIME=$(date +%s)
 
        # 执行验证
        RESULT=$($VALIDATE_CMD)
        echo "$RESULT"

        DEPLOY_STATUS=$(echo "$RESULT" | jq -r '.result.status')

        if [ "$DEPLOY_STATUS" = "Failed" ]; then
        echo "❌ 验证失败"
        exit 1
        fi

        STATUS=$(echo "$RESULT" | jq -r '.status')
        if [ "$STATUS" != "0" ]; then
          # 验证失败
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
 
          echo "❌ 验证失败 (耗时: ${DURATION}秒)"
          echo "validation-result=failed" >> $GITHUB_OUTPUT
          exit 1
          fi
          
          # 验证成功
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
 
          echo "✅ 验证成功 (耗时: ${DURATION}秒)"
          
          # 提取 deploy ID
          DEPLOY_ID=$(echo "$RESULT" | jq -r '.result.id')
          
          # 验证deploy ID
          if [ -z "$DEPLOY_ID" ] || [ "$DEPLOY_ID" = "null" ]; then
            echo "❌ 错误: 未能从响应中提取有效的部署 ID"
            echo "JSON 响应内容:$RESULT"
            exit 1
            fi

            echo "✅ 获取到部署 ID: $DEPLOY_ID"
            # 保存deploy ID到步骤输出,供实际部署quick deploy使用
            echo "deploy-id=$DEPLOY_ID" >> $GITHUB_OUTPUT
            
            # 保存test level和test class到步骤输出，供实际部署使用
            echo "test-level=$TEST_LEVEL" >> $GITHUB_OUTPUT
            echo "test-class=$TEST_CLASSES" >> $GITHUB_OUTPUT


  # 第三阶段：实际部署
  execute-deployment:
    name: 3. 实际部署
    runs-on: ubuntu-latest
    needs: configure-and-validate

    steps:
 
    # 步骤1：检出代码
    - name: 🔍 检出代码
      uses: actions/checkout@v4
 
    # 步骤2：设置Node.js环境
    - name: ⚙️ 设置Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 'latest'
 
    # 步骤3：安装Salesforce CLI
    - name: 📦 安装Salesforce CLI
      run: |
        echo "安装Salesforce CLI..."
        npm install -g @salesforce/cli
        sf --version
        sf plugins install @salesforce/plugin-packaging @salesforce/plugin-source --force
 
    # 步骤4：登录到Salesforce DEV02
    - name: 🔐 登录到Salesforce DEV02
      run: |
        # 登录到Salesforce
        echo "登录到dev02环境..."
        echo '${{ secrets.SFDX_DEV02_ORG_AUTH }}' > ./SFDX_ORG_AUTH.json
        sf org login sfdx-url --sfdx-url-file ./SFDX_ORG_AUTH.json --set-default
 
        echo "✅ 认证成功"

   # 步骤5：实际部署
    - name: 🚀 执行实际部署
      env:
        # 关键：传入验证部署阶段的DEPLOY_ID
        DEPLOY_ID: ${{ needs.configure-and-validate.outputs.DEPLOY_ID }}  
        TEST_LEVEL: ${{ needs.prepare-deployment.outputs.test-level }}
        TEST_CLASSES: ${{ needs.prepare-deployment.outputs.test-classes }}
      run: |
        echo "开始实际部署..."
        if [ -z "$DEPLOY_ID" ]; then
          echo "❌ 错误: 未找到部署 ID (DEPLOY_ID)"
          echo "请确保验证步骤成功执行并正确输出了 deploy-id"
          exit 1
        fi
  
          echo "使用验证阶段的部署 ID 进行快速部署: $DEPLOY_ID"
          echo "目标环境: DEV02"

        # 使用deploy start实际部署
        QUICK_DEPLOY_CMD="sf project deploy start --manifest manifest/package.xml --test-level $TEST_LEVEL"

        # 如果是指定测试类，追加 --tests 参数
        if [ "$TEST_LEVEL" = "RunSpecifiedTests" ] && [ -n "$TEST_CLASSES" ]; then
        QUICK_DEPLOY_CMD="$QUICK_DEPLOY_CMD --tests $TEST_CLASSES"
        fi
        QUICK_DEPLOY_CMD="$QUICK_DEPLOY_CMD -o ${{ vars.DEV02_USER_NAME }} --verbose --ignore-warnings"
        
        if eval $QUICK_DEPLOY_CMD; then
          echo "✅ 实际部署成功完成"
          echo "✅ 成功部署到目标salesforce环境"
        else
          echo "❌ 部署失败"
          exit 1
          fi
